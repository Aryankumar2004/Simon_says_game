nteractive Audio-Visual Feedback: Each color block flashes bright and plays a distinct hue animation when triggered by the system or clicked by the user.

Dynamic Level Tracking: The header instantly updates to track your current level and alerts you when a game-over state occurs.

Responsive Grid Layout: The 2x2 color block grid centers perfectly and adapts fluidly to mobile, tablet, and desktop screens.

State Management: Built with precise JavaScript logic to track user sequences against generated game arrays.

HTML5: Structures the game board, color grid container, and heading displays.

CSS3: Handles the dark-themed aesthetic, glowing box-shadow effects, center alignments (Flexbox/Grid), and custom @keyframes animations for the active flash state.

JavaScript (ES6): Controls the core game engine, including:

Event listeners for user clicks and keyboard inputs.

Randomized color sequence generation using Math.random().

Conditional checking algorithms for real-time validation of user inputs.
